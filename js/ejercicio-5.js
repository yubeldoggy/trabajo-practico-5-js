const minutos= +prompt(" ingrese el numero de minutos")

const segundos= minutos *60 

const horas= minutos / 60

alert(`${minutos} son equivalentes a ${segundos} segundos y en horas serian ${horas} horas`)